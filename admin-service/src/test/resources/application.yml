spring:
  datasource:
    url: jdbc:h2:mem:admin_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: none
  liquibase:
    enabled: false
  data:
    redis:
      repositories:
        enabled: false

eureka:
  client:
    enabled: false

jwt:
  secret: test-secret-key-test-secret-key-123456789
