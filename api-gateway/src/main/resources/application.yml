spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      server:
        webmvc:
          routes:
            - id: social-service
              uri: lb://social-service
              predicates:
                - Path=/api/posts/**,/api/comments/**
